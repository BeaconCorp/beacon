import React, { PropTypes } from 'react';

const BeaconInfo = React.createClass({
  render: () => {
    return (
      <div>This is the beacon info page</div>
    );
  },
});

export default BeaconInfo;
