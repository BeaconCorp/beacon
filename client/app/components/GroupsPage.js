import React from 'react';
import { PropTypes } from 'react';

var GroupsPage = React.createClass({

  render: () => {
    return (
      <div>This is the groups page</div>
    );
  },

});

module.exports = GroupsPage;
